const calculate = (data, buttonName) => {
  const { total, next, operation } = data;

  if (buttonName === "+/-") {

  }

  if (buttonName === "+") {

  }

  if (buttonName === "-") {

  }

  if (buttonName === "×") {
    
  }

  if (buttonName === "÷") {
    
  }

  if (buttonName === "%") {
    
  }

  if (buttonName === "AC") {

  }

  if (buttonName === "=") {
    
  }
}